# React Native / Metro expect a flat node_modules. pnpm's default isolated
# linker breaks Metro in clean installs (e.g. EAS Build): "Cannot read
# properties of undefined (reading 'transformFile')". Hoisted fixes it.
node-linker=hoisted
